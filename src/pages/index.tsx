import type { NextPage } from 'next'
import Head from 'next/head'

const Form: NextPage = () => (
  <div>
    <Head>
      <title>Form | Purple Metrics</title>
    </Head>
  </div>
)

export default Form
